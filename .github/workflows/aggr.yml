name: aggr
on:
  schedule: [{ cron: "7,37 * * * *" }]
  push: { branches: [main], paths: ["*.toml", themes/**, templates/**, static/**, .github/workflows/**] }
  workflow_dispatch:
permissions: { contents: write, pages: write, id-token: write, issues: write }
jobs:
  aggr:
    uses: aymericbeaumet/aggr/.github/workflows/aggr.yml@v1
